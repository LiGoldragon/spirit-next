[package]
name = "spirit-nexus"
version = "0.27.0"
edition = "2024"
publish = false

[dependencies]
spirit = { path = "../..", default-features = false }

[features]
testing-trace = ["spirit/testing-trace"]
agent-guardian = ["spirit/agent-guardian"]
